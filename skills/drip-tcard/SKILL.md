---
name: drip-tcard
description: "当用户提到 \"次卡\"、\"tcard\"、\"核销\"、\"发放次卡\"、\"转卡\"、\"分享次卡\"、\"次卡购买\" 时使用此 skill。"
---

# 水滴智店 - 次卡管理模块

次卡（Tcard）是水滴智店的预付费服务卡管理模块，支持购买、核销、发放、转卡、分享等完整生命周期操作。

## 认证方式

**Base URL:** `https://zd.drip.im/open/zd`

所有接口均为 **POST** 请求，`Content-Type: application/json`。

### 签名算法

```
sign = sha256(sha256(body + client_secret + ts) + client_secret)
```

请求 URL 需附加 query 参数：`client_id`、`ts`（Unix 时间戳）、`sign`。

凭证从环境变量 `DRIP_CLIENT_ID` 和 `DRIP_CLIENT_SECRET` 读取。

## API 端点（15 个）

| 路径 | 说明 |
|------|------|
| `/tcard/getById` | 根据 ID 读取次卡信息 |
| `/tcard/getByIds` | 批量读取次卡信息 |
| `/tcard/filter` | 根据条件（类目/服务）过滤次卡 |
| `/tcard/createOrder` | 创建次卡购买订单（未支付） |
| `/tcard/confirmOrder` | 确认订单支付成功 |
| `/tcard/refundOrder` | 次卡订单退款 |
| `/tcard/use` | 核销（使用）次卡 |
| `/tcard/send` | 直接发放次卡给客户 |
| `/tcard/transfer` | 次卡转卡至另一客户 |
| `/tcard/getUseRecord` | 获取次卡使用记录（分页） |
| `/tcard/getFetches` | 获取客户已领取的次卡列表 |
| `/tcard/getFetchCode` | 获取次卡二维码 |
| `/tcard/getShareRecord` | 获取次卡分享记录 |
| `/tcard/bindShare` | 绑定次卡分享关系 |
| `/tcard/unbindShare` | 解除次卡分享关系 |

## 错误码与处理指引

| 错误码 | 说明 | 处理方式 |
|--------|------|----------|
| `10003` | 无效请求 | 检查参数格式，不要重试 |
| `10004` | 服务器内部错误 | 可重试，间隔 3-5 秒 |
| `24001` | 次卡不存在 | 提示用户核实次卡 ID |
| `24002` | 次卡已过期 | 告知用户次卡已失效 |
| `24003` | 客户不存在 | 提示用户核实客户信息 |
| `24004` | 无效次卡 | 告知用户次卡状态异常 |
| `24005` | 次卡领取数量超过限制 | 告知用户已达上限 |
| `24006` | 订单不存在 | 提示用户核实订单 ID |
| `24007` | 无效订单 | 检查订单状态 |
| `24008` | 发放次卡异常 | 可重试，失败后报告 |
| `24009` | 当前有退款正在进行 | 等待退款完成后再操作 |
| `24010` | 退款金额超出限制 | 告知用户金额不合规 |
| `24011` | 支付方式不支持退款 | 告知用户该支付方式无法退款 |
| `24012` | 支付设置错误 | 建议联系管理员 |
| `24013` | 退款失败 | 可重试，失败后建议联系支持 |
| `24014` | 次卡领取记录不存在 | 提示用户核实信息 |
| `24015` | 核销服务不存在 | 提示用户核实服务 ID |
| `24016` | 服务次数不足 | 告知用户剩余次数不足 |
| `24017` | 领取记录状态错误 | 检查次卡当前状态 |
| `24018` | 使用时段错误 | 告知用户当前时段不可用 |
| `24019` | 领取记录已过期 | 告知用户记录已失效 |
| `24020` | 次卡使用频率超过限制 | 告知用户需等待 |
| `24021` | 使用异常 | 可重试，失败后报告 |
| `24022` | 发卡员工不存在 | 提示核实员工信息 |
| `24023` | 发卡员工不合法 | 提示核实员工权限 |
| `24024` | 次卡库存不足 | 告知用户库存不足 |
| `24025` | 转卡目标和来源相同 | 提示用户选择不同的目标客户 |
| `24027` | 次卡不支持分享 | 告知用户该次卡类型不支持 |
| `24028` | 次卡分享次数超过限制 | 告知用户已达分享上限 |
| `24029` | 次卡状态异常 | 检查次卡当前状态 |
| `24030` | 已绑定,请勿重复分享 | 告知用户已存在绑定关系 |

## 金额单位

所有金额字段单位为**分**（1 元 = 100 分），类型为 integer。

## 详细参数参考

完整的请求参数和响应字段定义，参见 `references/openapi-spec.yaml`。
